<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Category:</strong>
  <%= link_to(@product.category.name, category_path(@product.category.id)) %>
</p>

<p>
  <strong>Mfg:</strong>
  <%= @product.mfg %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Url:</strong>
  <a href="http://<%= @product.url %>" target="_blank"><%= @product.url %></a>
</p>

<p>
  <strong>Image:</strong>
  <a href="http://<%= @product.image %>" target="_blank"><%= @product.image %></a>
</p>

<p>
  <strong>User:</strong>
  <%= @product.user.email %>
</p>

<%= link_to 'Leave a Review!', new_product_review_path(@product) %><br>

<h2>Reviews:</h2>
<% @reviews.each do |f| %>
  <h4><%= f.title %></h4>
  <p><%= f.user.email %> <%= f.created_at %></p>
  <p><%= f.comment %></p>
  <p><%= f.rating %></p>
<% end %>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
